// Generated by CoffeeScript 1.11.1
(function() {
  var render_background, render_sprites;

  window.init_graphics = function(game_objs) {
    var new_div, sdata, sprite;
    for (sprite in game_objs["sprites"]) {
      sdata = game_objs["sprites"][sprite];
      new_div = $("<div>");
      new_div.css({
        position: "fixed",
        height: sdata["r"],
        width: sdata["r"],
        fill: sdata["img"]
      });
      $("body").append(new_div);
      sdata["div"] = new_div;
    }
    return $("body").css({
      height: "100vh",
      width: "100vw"
    });
  };

  window.render = function(game_objs) {
    render_background();
    return render_sprites(game_objs);
  };

  render_background = function() {};

  render_sprites = function(game_objs) {
    var results, s, sprite;
    results = [];
    for (sprite in game_objs["sprites"]) {
      s = game_objs["sprites"][sprite];
      results.push(s["div"].css({
        background: s["img"],
        left: s["cx"] - s["r"] / 2,
        top: s["cy"] - s["r"] / 2
      }));
    }
    return results;
  };

}).call(this);
